image: maven:3-openjdk-8

cache:
  paths:
    - .m2/repository

stages:
    - build
    - test
    - package
    - deploy


build_job:
    stage: build
    script:
        - echo "maven build started"
        - mvn compile

test_job:
    stage: test
    script:
        - echo "test started"
        - mvn started


package_job:
    stage: package
    script:
        - echo "package started"
        - mvn package


deploy_job:
    stage: deploy
    script:
        - echo "deploy started"
